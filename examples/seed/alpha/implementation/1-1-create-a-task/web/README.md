# implementation/1-1-create-a-task/web/ (example placeholder)

On a real metarepo this is a **git worktree** of the `web` clone, checked out on
branch `story/1-1-create-a-task`. It is gitignored; this placeholder exists only so the
`example` branch shows the layout a full-stack story produces.

Created on a real checkout with:

    bash scripts/bmad-router.sh worktree 1-1-create-a-task web api

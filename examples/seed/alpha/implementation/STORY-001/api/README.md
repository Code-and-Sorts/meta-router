# implementation/STORY-001/api/ (example placeholder)

On a real metarepo this is a **git worktree** of the `api` clone, checked out on
branch `story/STORY-001` — the same story branch as the sibling `web/` worktree.
It is gitignored; this placeholder exists only so the `example` branch shows the
layout a full-stack, multi-repo story produces.

Created on a real checkout with:

    bash scripts/bmad-router.sh worktree STORY-001 web api

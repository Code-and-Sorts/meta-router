# implementation/STORY-001/web/ (example placeholder)

On a real metarepo this is a **git worktree** of the `web` clone, checked out on
branch `story/STORY-001`. It is gitignored; this placeholder exists only so the
`example` branch shows the layout a full-stack story produces.

Created on a real checkout with:

    bash scripts/bmad-router.sh worktree STORY-001 web api

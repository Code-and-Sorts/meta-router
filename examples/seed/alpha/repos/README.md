# repos/ (example placeholder)

On a real metarepo this directory holds full git clones of the project's source
repos — here `web/` and `api/`, as declared in `../repos.yaml`. It is gitignored;
this placeholder exists only so the `example` branch shows the intended structure.

Populate it on a real checkout with:

    bash .claude/skills/meta-router/scripts/meta-router.sh clone

# repos/ (example placeholder)

On a real metarepo this holds the `cli` clone declared in `../repos.yaml`. It is
gitignored; this placeholder exists only so the `example` branch shows the
structure. Populate it with `bash .claude/skills/meta-router/scripts/meta-router.sh clone`.

# Workspace Skills

Place agent skill folders here. Each skill should be a directory with a
SKILL.md file. These skills are only active when this workspace is the
active meta-router context.

Example:
  workspaces/alpha/.claude/skills/
    my-api-skill/
      SKILL.md

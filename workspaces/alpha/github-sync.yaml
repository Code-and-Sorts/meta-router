# GitHub sync configuration for the "alpha" example workspace.
#
# For the live showcase, issues are created in this very repo so the feature is
# testable end-to-end. In a real metarepo this would point at alpha's own source repo.
repo: Code-and-Sorts/meta-router

# No Project board for the demo — issues sync without one. Run
# .claude/skills/meta-router/scripts/bmad-github-bootstrap.sh alpha to create a private board.
project: null
project_owner: null

labels:
  epic: epic
  story: story

planning: true
